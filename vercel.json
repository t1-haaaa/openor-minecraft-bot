{
  "framework": "nextjs",
  "buildCommand": "npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_OPENOR_API_URL": "@openor-api-url"
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" }
      ]
    }
  ],
  "notes": "Vercel is FRONTEND ONLY. No Minecraft bot processes, no persistent workers, no background jobs. Execution layer is independent."
}
